import { Wallet } from "./Wallet";

export class MetaMaskWallet extends Wallet {

    // async getAccout() : Promise<any[] | undefined> {
    //     return await this.provider.provider.request?.({method:"eth_requestAccounts", params:[]})
    // }
}

